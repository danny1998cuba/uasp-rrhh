import { TrabajadorFilterPipe } from './trabajador-filter.pipe';

describe('TrabajadorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TrabajadorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
