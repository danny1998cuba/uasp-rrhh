import { DepartamentoCargoPK } from './departamento-cargo-pk';

describe('DepartamentoCargoPK', () => {
  it('should create an instance', () => {
    expect(new DepartamentoCargoPK()).toBeTruthy();
  });
});
